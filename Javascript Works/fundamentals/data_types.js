var num=10;
var avg=10.5;
var name="hello";
var is_active=true;

console.log(`variable num type is ${typeof(num)}`);
console.log(`variable avg type is ${typeof(avg)}`);
console.log(`variable name type is ${typeof(name)}`);
console.log(`variable is_active type is ${typeof(is_active)}`);